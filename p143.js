// Bin a decimal
// omplete la función que convierte un número binario (dado como una cadena) en un número decimal.
const binToDec = (bin) => {
    // TODO
    
    return parseInt(bin, 2)
  }

  console.log(binToDec(['0']))
  console.log(binToDec(['1']))
  console.log(binToDec(['10']))
  console.log(binToDec(['1001001']))

//   La función parseInt() permite analizar una cadena y devolver su valor numérico en la base especificada.