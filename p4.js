// Invertir un arreglo:

// Enunciado: Escribe una función que invierta el orden de los elementos en un arreglo.
// Ejemplo de Entrada: [1, 2, 3, 4, 5]
// Ejemplo de Salida: [5, 4, 3, 2, 1]

const reverseOrder = (num) => {
    return num.reverse()
}

console.log(reverseOrder([1, 2, 3, 4, 5]))

