// Programa una funcion que dada un string te devuelva un array de textos separados por cierto caracter

const separateText = (str) => {
    return str.split(" ")
}

console.log(separateText("Hola que tal"))