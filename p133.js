// Función 2: elevar al cuadrado un argumento

// Ahora tienes que escribir una función que tome un argumento y devuelva su cuadrado.

const square = (num) => {
    return num * num 
}
console.log(square(3))