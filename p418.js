// Completa la función que devuelve una matriz de longitud n, comenzando con el número dado x y los cuadrados del número anterior. Si n es negativo o cero, devuelve una matriz/lista vacía.

const squares = (x, n) => {


};

console.log(squares(2, 5)); // => [2,4,16,256,65536]
console.log(squares(3, 3));
console.log(squares(5, 3));
console.log(squares(10, 4));